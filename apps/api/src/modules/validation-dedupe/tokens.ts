/** DI tokens for the validation-dedupe module. Symbols avoid string-token collisions. */
export const PRISMA = Symbol('VALIDATION_DEDUPE_PRISMA');
export const DUPLICATES_SERVICE = Symbol('DUPLICATES_SERVICE');

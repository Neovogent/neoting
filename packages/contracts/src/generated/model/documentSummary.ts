/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Neoting API
 * The single API. The public API is this same API with scoped OAuth clients —
there is no second door (D10, Governance §3).

Money is integer pence. Nothing changes state outside the Review → Approve
path. Tenancy is row-level security, never the URL. Every extracted value
carries its provenance class. Errors are RFC 7807 `problem+json` with a
stable `NT-` code.

The reasoning behind each of those, and the conventions a consumer needs, are
in `packages/contracts/CLAUDE.md` and in the header comment of
`openapi.yaml`. They are deliberately not repeated here: this field is
stamped into the header of every generated file.

 * OpenAPI spec version: 0.1.0-draft
 */
import type { Inbox } from './inbox';
import type { DocumentState } from './documentState';
import type { DocumentSummaryDocType } from './documentSummaryDocType';
import type { DocumentChannel } from './documentChannel';
import type { DocumentSummarySupplierName } from './documentSummarySupplierName';
import type { DocumentSummaryCustomerName } from './documentSummaryCustomerName';
import type { DocumentSummaryDocumentDate } from './documentSummaryDocumentDate';
import type { DocumentSummaryDueDate } from './documentSummaryDueDate';
import type { DocumentSummaryCurrency } from './documentSummaryCurrency';
import type { DocumentSummaryTotalPence } from './documentSummaryTotalPence';
import type { DocumentSummaryTaxPence } from './documentSummaryTaxPence';
import type { DocumentSummaryReference } from './documentSummaryReference';
import type { DocumentSummaryCategoryCode } from './documentSummaryCategoryCode';
import type { DocumentSummaryDescription } from './documentSummaryDescription';
import type { DocumentSummaryProjectRef } from './documentSummaryProjectRef';
import type { DocumentSummaryParentDocumentId } from './documentSummaryParentDocumentId';
import type { DocumentSummaryFailureCode } from './documentSummaryFailureCode';
import type { DocumentSummaryFailureMessage } from './documentSummaryFailureMessage';
import type { DocumentSummaryArchivedAt } from './documentSummaryArchivedAt';

/**
 * The row shape for inbox lists and tables. Header fields are a projection
of the accepted extraction, denormalised so a list of 500 rows does not
reach into extraction JSON 500 times.

 */
export interface DocumentSummary {
  id: string;
  businessId: string;
  inbox: Inbox;
  state: DocumentState;
  docType?: DocumentSummaryDocType;
  channel: DocumentChannel;
  originalFilename: string;
  receivedAt: string;
  supplierName?: DocumentSummarySupplierName;
  customerName?: DocumentSummaryCustomerName;
  /** A calendar date, not an instant. UK d/m/y is disambiguated at parse time, never here. */
  documentDate?: DocumentSummaryDocumentDate;
  dueDate?: DocumentSummaryDueDate;
  /**
   * ISO 4217. Locked to document evidence; a change raises an alert rather than silently flipping (SoT Stage 2).
   * @pattern ^[A-Z]{3}$
   */
  currency?: DocumentSummaryCurrency;
  /** Gross total in pence. */
  totalPence?: DocumentSummaryTotalPence;
  taxPence?: DocumentSummaryTaxPence;
  reference?: DocumentSummaryReference;
  categoryCode?: DocumentSummaryCategoryCode;
  description?: DocumentSummaryDescription;
  projectRef?: DocumentSummaryProjectRef;
  /** Set when this document came out of an auto-split batch. */
  parentDocumentId?: DocumentSummaryParentDocumentId;
  /** Never null when `state` is `REJECTED` or `FAILED`. */
  failureCode?: DocumentSummaryFailureCode;
  /** Plain English, and specific. "Password-protected PDF" — not "processing error". */
  failureMessage?: DocumentSummaryFailureMessage;
  /** Whether the Rejected/Failed view should offer a retry. Retrying is
itself an ActionProposal of kind `document.reprocess`.
 */
  retryable: boolean;
  archivedAt?: DocumentSummaryArchivedAt;
}

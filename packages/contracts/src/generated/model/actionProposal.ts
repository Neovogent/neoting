/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Neoting API
 * The single API. The public API is this same API with scoped OAuth clients —
there is no second door (D10, Governance §3).

Money is integer pence. Nothing changes state outside the Review → Approve
path. Tenancy is row-level security, never the URL. Every extracted value
carries its provenance class. Errors are RFC 7807 `problem+json` with a
stable `NT-` code.

The reasoning behind each of those, and the conventions a consumer needs, are
in `packages/contracts/CLAUDE.md` and in the header comment of
`openapi.yaml`. They are deliberately not repeated here: this field is
stamped into the header of every generated file.

 * OpenAPI spec version: 0.1.0-draft
 */
import type { ActionProposalBusinessId } from './actionProposalBusinessId';
import type { ProposalKind } from './proposalKind';
import type { ProposalState } from './proposalState';
import type { ActionProposalPayload } from './actionProposalPayload';
import type { ActionProposalRenderedSummaryHash } from './actionProposalRenderedSummaryHash';
import type { ActionProposalCreatedByUserId } from './actionProposalCreatedByUserId';
import type { ActionProposalCreatedByModel } from './actionProposalCreatedByModel';
import type { ActionProposalReviewedAt } from './actionProposalReviewedAt';
import type { ActionProposalApprovedByUserId } from './actionProposalApprovedByUserId';
import type { ActionProposalApprovedAt } from './actionProposalApprovedAt';
import type { ActionProposalExecutedAt } from './actionProposalExecutedAt';
import type { ActionProposalPolicyProposalId } from './actionProposalPolicyProposalId';
import type { ActionProposalOutcome } from './actionProposalOutcome';
import type { ActionProposalTraceId } from './actionProposalTraceId';

export interface ActionProposal {
  id: string;
  businessId?: ActionProposalBusinessId;
  kind: ProposalKind;
  state: ProposalState;
  /** What was proposed, as submitted. */
  payload?: ActionProposalPayload;
  /**
   * SHA-256 over the canonical payload. Written to the audit event on approval.
   * @pattern ^[a-f0-9]{64}$
   */
  payloadHash: string;
  /** Null until the review has been opened. */
  renderedSummaryHash?: ActionProposalRenderedSummaryHash;
  createdByUserId?: ActionProposalCreatedByUserId;
  /** Set when a model proposed this. A model may create a proposal and
may never approve one — the two fields are never both a model.
 */
  createdByModel?: ActionProposalCreatedByModel;
  createdAt: string;
  /** Set by the review endpoint alone. While this is null the server
refuses approval, whatever the UI shows.
 */
  reviewedAt?: ActionProposalReviewedAt;
  approvedByUserId?: ActionProposalApprovedByUserId;
  approvedAt?: ActionProposalApprovedAt;
  executedAt?: ActionProposalExecutedAt;
  expiresAt: string;
  /** Set on automated executions, naming the standing policy they ran
under. A standing automation runs without a per-item proposal only
because the policy itself was approved through this same contract
(Governance §10.5).
 */
  policyProposalId?: ActionProposalPolicyProposalId;
  /** What actually changed. Populated on execution. */
  outcome?: ActionProposalOutcome;
  traceId?: ActionProposalTraceId;
}

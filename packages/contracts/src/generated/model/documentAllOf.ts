/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Neoting API
 * The single API. The public API is this same API with scoped OAuth clients —
there is no second door (D10, Governance §3).

Money is integer pence. Nothing changes state outside the Review → Approve
path. Tenancy is row-level security, never the URL. Every extracted value
carries its provenance class. Errors are RFC 7807 `problem+json` with a
stable `NT-` code.

The reasoning behind each of those, and the conventions a consumer needs, are
in `packages/contracts/CLAUDE.md` and in the header comment of
`openapi.yaml`. They are deliberately not repeated here: this field is
stamped into the header of every generated file.

 * OpenAPI spec version: 0.1.0-draft
 */
import type { DocumentAllOfPerceptualHash } from './documentAllOfPerceptualHash.js';
import type { DocumentAllOfSubmitterUserId } from './documentAllOfSubmitterUserId.js';
import type { DocumentAllOfSubmitterLabel } from './documentAllOfSubmitterLabel.js';
import type { DocumentAllOfReceivedLocal } from './documentAllOfReceivedLocal.js';
import type { DocumentAllOfRoutingDecision } from './documentAllOfRoutingDecision.js';
import type { DocumentAllOfRoutingConfidence } from './documentAllOfRoutingConfidence.js';
import type { DocumentAllOfPageRange } from './documentAllOfPageRange.js';
import type { DocumentAllOfAcceptedExtraction } from './documentAllOfAcceptedExtraction.js';

export type DocumentAllOf = {
  /** The type decided by magic-byte sniffing, never the one the client declared. */
  mimeType: string;
  byteSize: number;
  /** @pattern ^[a-f0-9]{64}$ */
  byteHash: string;
  /** Catches the same paper photographed twice, cropped or rotated —
one of the multi-signal dedupe nets (SoT Stage 6).
 */
  perceptualHash?: DocumentAllOfPerceptualHash;
  submitterUserId?: DocumentAllOfSubmitterUserId;
  /** Display name for a submitter with no login — someone who emails or
WhatsApps documents is recorded as Document Owner without ever
being provisioned as a user.
 */
  submitterLabel?: DocumentAllOfSubmitterLabel;
  /** The Europe/London wall-clock time as received, kept for evidence alongside the UTC instant. */
  receivedLocal?: DocumentAllOfReceivedLocal;
  /** How this document reached this workspace: which rule matched, or
which model inferred the addressee and from what evidence. Null
when the sender identity matched exactly and no inference was
needed. This is the "show the working" payload for the routing
step (§13.3).
 */
  routingDecision?: DocumentAllOfRoutingDecision;
  /**
   * Model-reported, for display and triage only. It never gates
execution — thresholds come from eval measurement, not from a
model's opinion of itself (Governance §9.5).

   * @minimum 0
   * @maximum 1
   */
  routingConfidence?: DocumentAllOfRoutingConfidence;
  /** Which pages of the parent this document came from, e.g. "4-6". */
  pageRange?: DocumentAllOfPageRange;
  acceptedExtraction?: DocumentAllOfAcceptedExtraction;
  createdAt: string;
  updatedAt: string;
};

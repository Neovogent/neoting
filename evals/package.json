{
  "name": "@neoting/evals",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo \"lint: covered by the root eslint config\"",
    "test": "echo \"test: the eval suite runs under test:eval, not under unit tests\"",
    "test:eval": "tsx src/run-chat-evals.ts"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  }
}
